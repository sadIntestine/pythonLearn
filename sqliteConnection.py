# See PyCharm help at https://www.jetbrains.com/help/pycharm/
# 导入SQLite驱动

from utils.db import DbCommit
from utils.db import DbQuery
class Db():
    def excuteInsert(tableName, column=(), columnValues=()):

        insertSql = 'insert into'
        return

    @DbQuery()
    def isExist(tableName):

        return '\'insert into weather(day,weather,highTemp,lowTemp) values (%s, %s, %s, %s)\',(6日（今天）,多云,17,5)'
